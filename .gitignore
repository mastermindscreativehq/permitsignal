# Nested repo -- dashboard/ manages its own git history independently.
dashboard/

# Python
.venv/
__pycache__/
*.pyc

# Secrets
.env

# Pipeline output (regenerated by running the pipeline)
data/output/

# Discovery registry state (regenerated bookkeeping, not source content)
data/state/
